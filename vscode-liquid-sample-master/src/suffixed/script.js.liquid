const x = function(

      param         ) {

  const x = {{ object.prop[1] | abs }};

{% if condition %}

    const o = [
      {% for i in arr %}        "{{ i }}",{% endif %}



    ];  {% endif %}

  param["string"][20]
  window.prop()

  const c = new Class()

}
